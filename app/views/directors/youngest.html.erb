<!-- app/views/directors/youngest.html.erb -->


<h1>Youngest director</h1>

<% if @youngest_director %>
  <p>
    The youngest director in our dataset right now is 
    <%= link_to @youngest_director.name, director_path(@youngest_director) %>,
    born on <%= @youngest_director.dob.strftime("%B %d, %Y") %>.
  </p>
<% end %>
